# Database Configuration
DB_HOST=127.0.0.1          # MySQL database host (use localhost or an IP address)
DB_USER=root               # MySQL username
DB_PASSWORD=aquib123  # MySQL password
DB_NAME=job_portal # MySQL database name

# JWT Secret Key
SECRET_KEY=mysecretkey  # Secret key for signing JWTs

# Server Configuration
PORT=5000                 # Server port (optional, defaults to 5000)

# Optional Variables
TOKEN_EXPIRY=1h           # Token expiration time (e.g., 1h for 1 hour, 7d for 7 days)
